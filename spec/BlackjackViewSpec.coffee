assert = chai.assert

describe "Blackjack View", ->

  it "should always pass...", ->
    assert.strictEqual true, true

  it "should add a card to playerHand when player hits", ->
    # Hand.prototype.hit()
  